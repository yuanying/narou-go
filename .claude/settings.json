{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -e '.tool_input.file_path | endswith(\".go\")' > /dev/null && (cd \"$(git rev-parse --show-toplevel)\" && go tool golangci-lint run ./...)"
          }
        ]
      }
    ]
  }
}
